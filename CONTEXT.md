# Doc Reader

轻量级本地多目录文档阅读器：浏览、阅读、搜索配置根下的 Markdown / 文本文件。

## Language

**跨库搜索**:
在所有已配置文档根下，按文件名与正文内容检索文档，并从结果打开目标文档的入口（`⌘K` / 侧栏搜索）。
_Avoid_: 全局搜索（易与「全站索引」混淆）, 全文索引

**文内查找**:
在当前已打开文档的**阅读视图**中查找、高亮并上下跳转匹配（`Ctrl+F`）。
_Avoid_: 页面搜索, 浏览器自带查找（指应用内能力时）

**阅读视图**:
将文档渲染为可读 HTML 后的主阅读界面（非源码）。
_Avoid_: 预览区（易与编辑预览混淆）, 渲染层

**源码视图**:
展示磁盘上的原始文本（Markdown 源），不作为跨库定位的默认落点。
_Avoid_: 原文, raw

**文档根**:
用户配置的一个可浏览目录根（`directories` 中的一项），跨库搜索与树浏览均受其边界约束。
_Avoid_: 仓库, workspace（除非特指产品名）

**命中**:
一次跨库搜索结果与查询的对应关系；可为文件名命中或正文命中。
_Avoid_: 搜索结果行（UI 用语，非领域概念）
